# Backend Environment Configuration
# Optimized for 16GB RAM, Intel i7-1065G7 (4 cores/8 threads)

# ===== CORS Settings =====
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://192.168.19.53:5173,http://192.168.19.53:8000

# ===== Ollama Configuration (Optimized for 16GB RAM system) =====
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
OLLAMA_TIMEOUT=60                    # 60s per article (increased for safety)
OLLAMA_TOTAL_TIMEOUT=480             # 8 minutes total (sequential processing)

# ===== Scraping Limits (Global Defaults) =====
# These can be overridden per-source in config/sources.yaml
MAX_SEARCH_RESULTS=10                # Maximum URL results to extract from search page
MAX_ARTICLES_TO_PROCESS=1            # Maximum articles to scrape and process with LLM (set to 1 for 16GB RAM)

# ===== Performance (Optimized for 16GB RAM, 4-core CPU) =====
MAX_CONCURRENT_SCRAPES=3             # Reduced: 3 parallel web scrapes (was 10)
MAX_CONCURRENT_LLM=1                 # CRITICAL: Only 1 LLM at a time to prevent hanging!
LOG_LEVEL=INFO
